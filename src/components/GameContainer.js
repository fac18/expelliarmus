import React from "react";
import Player from "./Player";
import TextOverlay from "./TextOverlay";

const GameContainer = ({ user }) => {
  return <p>Hi I'm the Game Container</p>;
};

export default GameContainer;
